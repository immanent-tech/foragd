// Copyright 2026 Joshua Rich <joshua.rich@gmail.com>.
// SPDX-License-Identifier: 	AGPL-3.0-or-later

package templates

import (
	"github.com/immanent-tech/foragd/models"
	"github.com/immanent-tech/foragd/web/templates/partials"
	"slices"
)

templ PostsIndex(posts []*models.MarkdownFile) {
	@content() {
		@container() {
			<div class="mx-auto max-w-2xl lg:mx-0">
				@partials.Heading2() {
					Posts
				}
				@partials.Paragraph() {
					Posts from the Foragd team.
				}
			</div>
			@grid(
				WithClasses("mt-8 place-items-start sm:min-h-svh"),
			) {
				for post := range slices.Values(posts) {
					<a href={ "/posts/" + post.Details.Path }>
						<article class="card card-border bg-base-200 max-w-prose h-fit cursor-pointer">
							<div class="card-body">
								<h2 class="card-title">{ post.Frontmatter.Title }</h2>
								<p>{ post.Frontmatter.Description }</p>
								<div class="card-actions justify-end">
									<time datetime={ post.GetTimestamp() } class="label">{ post.GetTimestamp() }</time>
								</div>
							</div>
						</article>
					</a>
				}
			}
			@partials.ButtonBackToTop()
		}
	}
}
